from hevi_proto.pyild import task
from hevi_util.program import Program
from glob import glob

python = Program("python3")

setup = Program("python3","setup.py")

modules = sorted(glob("hevi_proto/*.py"))

@task
def develop():
  setup("develop")
  
@task
def develop():
  python("./etc/doccat.py",modules,">README.rst",shell=True)
